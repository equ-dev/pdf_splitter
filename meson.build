project('pdf-chapter-splitter', 'c',
  version : '0.1.0',
  default_options : ['warning_level=2', 'c_std=gnu11'])

gtk_dep = dependency('gtk4')
poppler_dep = dependency('poppler-glib')

subdir('src')
subdir('tests')
subdir('bench')
